/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['picsum.photos', 'img-c.udemycdn.com', 'cdn.freecodecamp.org'],
  },
}

module.exports = nextConfig

